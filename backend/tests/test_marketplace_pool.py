"""Marketplace pool service tests — skipped until v2 DimMarketplace migration."""

import pytest


@pytest.mark.skip(reason="Pending migration to v2 schema")
async def test_placeholder():
    assert True
