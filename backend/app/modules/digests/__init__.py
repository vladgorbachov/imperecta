"""Digests module - intentionally empty.

The previous v2-migration stubs have been deleted (DA1). The module is reserved
for a future digests implementation; until then it exposes no routes, services,
schemas, or Celery tasks.
"""
